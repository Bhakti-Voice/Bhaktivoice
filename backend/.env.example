ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me
SESSION_SECRET=change-this-session-secret
SITE_ORIGIN=http://localhost:3000

# Same Turso database the public site uses.
# libsql:// is converted to https:// on Vercel so the connection does not hang.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=
